<%= react_component 'MemoriesForm', date: !@date.blank? && @date.strftime("%d.%m.%Y"),
                                    memories: MemoSpansSerializer.new(@memoes,
                                                    total: @memoes.total_size,
                                                    page: @page,
                                                    locales: @locales,
                                                    serializer: MemoSpanSerializer,
                                                    date: @date,
                                                    julian: @julian,
                                                    calendaries: @calendary_slugs),
                                    calendaries_cloud: CalendaryCloudSerializer.new( @calendary_cloud, locales: @locales ),
                                    calendaries_used: @calendary_slugs,
                                    query: @query %>
